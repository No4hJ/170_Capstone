using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S2Start : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Global.Scene = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
